[build]
  command = "pip install -r requirements.txt && flask run --host=0.0.0.0 --port=8080" # Inicie o Flask diretamente
  publish = "build"  # Diretório onde os arquivos estáticos são gerados

[build.environment]
  FLASK_APP = "app.py"
  FLASK_ENV = "production"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200




